import type { ConfigType } from '@stitches/react/types/config'

export const media: ConfigType.Media = {
  sm: '(min-width: 425px)',
  md: '(min-width: 768px)',
  lg: '(min-width: 1024px)',
  xl: '(min-width: 1440px)'
}
